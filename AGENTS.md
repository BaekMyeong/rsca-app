# RSCA App (마음챙김 진단 앱) - 프로젝트 컨텍스트

## 기본 정보
- **프로젝트명**: rsca-app
- **설명**: 명코칭 REACH 마음챙김 자가 진단 앱 (Homepage-main이나 kseha-main 등 기존 저장소와 완전히 분리된 독립 저장소)

## 배포 및 인프라
- **원격 저장소 (GitHub)**: https://github.com/BaekMyeong/rsca-app.git
- **호스팅/배포 (Vercel)**: https://rsca-app.vercel.app
- **자동 배포 파이프라인**: GitHub 저장소의 `master` 브랜치에 코드가 푸시되면 Vercel이 이를 감지하여 자동으로 빌드 및 프로덕션 환경에 배포합니다. (`vercel git connect` 설정 완료)

## 배포 방법 (How to Deploy)
로컬에서 기능 수정 및 테스트(개발 서버 `npm run dev`)를 완료한 후, 터미널에서 다음 명령어를 실행하여 GitHub에 코드를 푸시하면 자동으로 배포됩니다.
```bash
git add .
git commit -m "수정 내용 요약"
git push
```
별도의 Vercel 명령어를 실행할 필요 없이, `git push` 만으로 운영 서버에 변경사항이 즉시 반영됩니다.

## 아키텍처 및 주요 파일
- **프론트엔드 스택**: React + Vite + Vanilla CSS
- `src/App.jsx`: UI 렌더링, 상태 관리(문항 진행, 결과 렌더링 등), 화면 분기 담당
- `src/logic/rscaLogic.js`: 핵심 채점 로직, 동점자 우선순위 처리(최저점 도메인 추출 등), 페르소나 및 레벨별 코멘트 데이터 제공
- `src/index.css`: 컬러 톤앤매너, 타이포그래피, 커스텀 버튼/카드 레이아웃 정의 (Tailwind 사용 안 함)
- **리포트 캡처 기능**: `html2canvas` 라이브러리를 활용하여 결과 카드만 캡처 후 이미지로 저장

## 주요 디자인 원칙 및 사용자 경험 (UX/UI)
- 톤앤매너: 쨍한 원색이나 경고성 컬러를 지양하고, 따뜻하고 고급스러운 베이지/딥 네이비/웜 테라코타 컬러를 사용.
- 감성 문구: "힘내세요", "응원합니다" 같은 상투적 표현을 배제하고, 현존과 자각 중심의 정갈한 문장 사용.
- 직관성: 동점자 발생 시 혼동을 줄이는 안내 문구("* 가장 집중적인 충전이 필요한 영역...") 제공 및 점수 옆 기준 점수(/30) 병기.
- 정보 계층: 핵심 결과와 감성 총평은 최상단에, 이미지 저장 같은 보조 액션과 외부 링크(코칭/도서)는 하단 카드 영역에 정돈되게 배치.
